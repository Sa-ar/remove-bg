NEXT_PUBLIC_API_URL=https://api.rembg.site
UI_TOKEN_SECRET=dev-ui-token-secret-change-me

# Neon Postgres (pooled URL) for dashboard / DB-backed keys
DATABASE_URL=

# Neon Auth (Managed Better Auth) — from Console → Branch → Auth
NEON_AUTH_BASE_URL=
# openssl rand -base64 32
NEON_AUTH_COOKIE_SECRET=
