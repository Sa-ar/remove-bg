# Hugging Face Spaces: copy/symlink this folder as the Space root, or set
# Dockerfile path to apps/api/Dockerfile when connecting a monorepo.
#
# Required secrets: API_KEYS, UI_TOKEN_SECRET, WEB_ORIGIN
# Hardware: CPU Basic (16GB RAM)
